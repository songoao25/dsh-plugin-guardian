# dsh-plugin-guardian bundle patch: mounts the plugin into a profile layer stack.
# The plugin row id is 'dsh-plugin-guardian' — users may override its behavior in
# their profile's cordis.patch.yml with a row carrying the same id.
- insert:
    - id: dsh-plugin-guardian
      name: 'dsh-plugin-guardian'
